## Station
- 역이름 유효성 검사
- 이름이 같을 경우 같은 객체 취급

## Line
- 상행 하행 종점
- 노선이름 유효성 검사


## StationRepository

## LineRepository


## StationService
- 등록기능 (역이름 유효성 검사)
- 삭제 기능  (노선 등록 여부 검사)

##LineService
- 등록기능 (노선 이름 유효성 검사, 상행 하행 역 존재 여부 검사)
- 삭제 기능 (해당 역의 등록 정보를 모두 지운다)
- 조회기능

## 구간Service
- 구간등록
--노선 존재 여부, 역 존재여부, 등록 가능 여부 검사(인덱스 검사)
--역 개수 유효성 검사
-- 역과 역 사이에만 추가

- 구간 삭제
-- 노선 존재 여부, 역 존재여부, 삭제 가능 여부 검사(역 개수)
-- 종점 업데이트, 해당 노선 역 개수 업데이트
